services:
  react-hw:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_API_BASE_URL: https://taltech.akaver.com/api/v1
        NEXT_PUBLIC_BASE_PATH: /react-hw
    ports:
      - "3000:3000"
    restart: unless-stopped
