# MyProjectUI

Standalone JavaScript UI extracted from `MyProject/JsClient`.

This folder now contains the frontend files and Docker image definition used by the backend compose files in `../MyProject`.
